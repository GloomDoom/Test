git config --global user.name "GloomDoom"
git config --global user.email "dumglum@gmail.com"

git config --list посмотреть настройки

git help commit
git commit --help

Создание
cd D:/Rep/Test
git init

git status
git add
git add . все файлы
git add file.txt один файл

git commit -m "comment"

git remote add origin https://github.com/GloomDoom/Test.git добавить удаленный репозиторий
git push -u origin master толкнуть в репозиторий